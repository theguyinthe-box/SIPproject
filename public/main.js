document.addEventListener('keydown', function(event) {
    if(event.key == 'ArrowLeft') {
        alert('Left was pressed');
    }
    else if(event.key == 'ArrowRight') {
        alert('Right was pressed');
    }
});

function makeSelection(userID, imageShown, userSelection){
        
}